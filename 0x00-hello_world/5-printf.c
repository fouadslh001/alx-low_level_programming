#include <stdio.h>
/**
 * main - first print with betty with printf
 * Return: the return is 0 that means its sucsese
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
